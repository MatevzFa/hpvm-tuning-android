# Path to your copy of the HPVM repository
export HPVM_ROOT="path/to/hpvm-release"

# Path to the model_params directory containing NN parameters for pretrained neural networks.
export MODEL_PARAMS_DIR="$HPVM_ROOT/"

# Path to the global knobs file
export GLOBAL_KNOBS_PATH="$HPVM_ROOT/hpvm/projects/hpvm-tensor-rt/global_knobs.txt"

# Path to the build directory where the hpvm-tensor-rt-android is built.
export ANDROID_HPVM_TENSOR_RT_BUILD_DIR=""
